investor:{what:"Training is lumpy and frontier-driven; inference is recurring and tied to adoption, task depth and agent activity.",priced:"The market may extrapolate training clusters while underestimating inference, or declare training finished while new modalities and model generations continue.",indicators:"Training run cadence, model size/architecture, inference token growth, reasoning length, tool calls, enterprise deployment and model refresh cycles.",trap:"Assuming parameter count is a complete measure of capability, cost or demand.",secondOrder:"Better post-training or retrieval can reduce the need for a larger model; better models can also unlock more valuable and longer-running tasks.",invalidation:"A compute-demand thesis weakens if capability gains decouple from scale and user value fails to rise with additional inference effort."},quiz:[{q:"Which statement best distinguishes RAG from fine-tuning?",options:["RAG changes weights; fine-tuning adds documents","RAG supplies information at runtime; fine-tuning changes weights","They are identical","Neither can use private data"],answer:1,explanation:"RAG inserts retrieved context into the current request; fine-tuning updates model parameters through additional training."},{q:"Why can an MoE model have many total parameters but lower compute per token than a dense model of the same size?",options:["It deletes the tokenizer","Only a subset of experts is active for a token","It stores no weights","It runs only on CPUs"],answer:1,explanation:"Sparse routing activates only selected experts, although the total weights still need to be stored and communicated efficiently."},{q:"Test-time compute refers primarily to:",options:["Electricity used to manufacture chips","Extra computation used while solving a live request","Only pretraining data cleaning","The time needed to install a server"],answer:1,explanation:"Reasoning tokens, search, tool calls and verification loops consume computation during inference."}]},{id:4,stage:"Foundation",time:"40 min",title:"Inference mechanics",subtitle:"Prefill, decode, KV cache, batching and token economics",summary:"Learn why one word ‘inference’ hides several workloads with different hardware, latency and margin characteristics.",analogy:"Prefill is reading and indexing the entire case file before a meeting. Decode is speaking the answer one word at a time while repeatedly consulting that index. A long file makes preparation heavy; many simultaneous clients make the index storage and scheduling difficult.",objectives:["Distinguish time-to-first-token from inter-token latency and total throughput.","Explain prefill, decode and the KV cache.","Understand how batching, caching, quantization, speculative decoding and routing change cost."],plain:[{title:"Prefill processes the prompt",body:"The model reads the input tokens in parallel and builds internal representations plus a KV cache. Long prompts and retrieved documents increase prefill work and time-to-first-token."},{title:"Decode generates sequentially",body:"After prefill, the model produces one token, then uses that token and the existing cache to produce the next. This sequential loop is difficult to parallelise across time and often becomes memory-bandwidth sensitive."},{title:"The same model can have different economics",body:"A short classification, a long legal document, a coding agent and a real-time voice assistant have different input/output ratios, latency targets, context lengths, tool calls and concurrency. Average cost per token can conceal the mix."}],mechanics:[{title:"KV cache: the working memory of the conversation",body:"Attention reuses key and value tensors from earlier tokens. Storing them avoids recomputing the full history for every new token, but the cache grows with sequence length, batch size, layer count and hidden dimensions. It can become the capacity bottleneck for long context and high concurrency."},{title:"Core optimisation levers",bullets:["<strong>Batching:</strong> combine requests to raise accelerator utilization, at the risk of latency.","<strong>Quantization:</strong> use fewer bits for weights or cache, reducing memory and bandwidth.","<strong>Prompt/KV caching:</strong> reuse common prefixes or previous work.","<strong>Speculative decoding:</strong> draft several tokens cheaply and verify them with the main model.","<strong>Model routing:</strong> send easy tasks to a cheaper model and difficult tasks to a frontier model.","<strong>Prefill/decode disaggregation:</strong> place the two phases on separately optimised resources."]},{title:"Price deflation is not demand destruction",body:"The relevant identity is total spend = price per token × tokens per task × number of tasks. All three terms move. Agents, multimodality and long context can raise tokens per task even as model and hardware efficiency lower unit cost."}],equation:{formula:"AI spend = tasks × tokens per task × price per token",note:"For a vendor, replace price with revenue per token or task and separately model compute cost, cache savings, model mix and utilization. The second derivative of each term matters for equities."},terms:["inference","prefill","decode","KV cache","TTFT","ITL","batching","quantization","speculative decoding","routing"],investor:{what:"Track useful work per request and the cost to serve it, not a single token-price series.",priced:"Consensus often treats token deflation as either universally bearish for infrastructure or universally bullish for adoption. The elasticity and workload mix decide.",indicators:"Input/output token mix, context length, cache hit rate, reasoning tokens, concurrency, utilization, latency SLA and model routing share.",trap:"Using published API price as a proxy for underlying cost or vendor gross margin.",secondOrder:"Cheaper inference can expand usage enough to tighten memory, power and network demand while compressing model-provider margins.",invalidation:"An inference-volume bull case fails if lower prices do not unlock valuable tasks or if software captures efficiency gains without passing them into usage."},quiz:[{q:"Which phase usually processes the prompt tokens in parallel?",options:["Decode","Prefill","Packaging","Fine-tuning"],answer:1,explanation:"Prefill processes the input context and creates the KV cache; decode then generates tokens sequentially."},{q:"Why does long context pressure memory capacity?",options:["The tokenizer becomes a CPU","The KV cache grows with sequence length and concurrency","HBM loses bandwidth","Weights disappear"],answer:1,explanation:"The cache stores attention state for prior tokens across layers and requests."},{q:"Token prices fall 70%, tasks rise 6× and tokens per task double. What happens to total token-linked spend, all else equal?",options:["Falls 70%","Rises 3.6×","Is unchanged","Cannot exceed 1×"],answer:1,explanation:"0.30 × 6 × 2 = 3.6. Unit deflation can coexist with strong aggregate growth."}]},{id:5,stage:"Hardware",time:"40 min",title:"Memory: SRAM, DRAM, HBM and storage",subtitle:"Capacity, bandwidth, latency and the memory wall",summary:"AI performance depends as much on moving data as on arithmetic. Learn the hierarchy and why HBM is valuable even though it is still DRAM.",analogy:"A register is the item already in your hand. SRAM is the desk drawer. HBM is a large bookcase pressed against the desk with very wide doors. Server DDR is the storeroom across the corridor. NAND storage is the warehouse downstairs. Bigger and cheaper tiers are generally farther away and slower to reach.",objectives:["Separate memory capacity, bandwidth, latency and energy per bit moved.","Explain why model weights and KV cache create different memory demands.","Understand the hierarchy from on-chip SRAM to HBM, DDR and NAND."],plain:[{title:"Four questions for any memory claim",body:"Ask: <strong>How much fits?</strong> (capacity), <strong>how fast can it flow?</strong> (bandwidth), <strong>how long until the first byte arrives?</strong> (latency), and <strong>how much power/cost does movement consume?</strong> A memory can be excellent on one dimension and weak on another."},{title:"SRAM versus DRAM",body:"SRAM is fast and used for caches close to compute, but each bit takes more silicon area. DRAM is denser and cheaper per bit but must be refreshed and is slower. HBM is DRAM packaged to deliver exceptionally wide, high-bandwidth access near the accelerator."},{title:"NAND is persistent storage",body:"NAND flash retains data without power and is far cheaper per bit, but it cannot feed accelerator arithmetic at HBM speeds. It stores datasets, checkpoints, embeddings and colder KV states; data must be staged upward before use."}],mechanics:[{title:"Weights and cache compete for scarce fast memory",body:"Model weights consume capacity roughly in proportion to parameter count × bytes per parameter. During inference, activations and the KV cache add to the footprint. Quantization can shrink weights; grouped-query attention and cache compression can reduce KV requirements."},{title:"Why decode can be bandwidth-bound",body:"For a low-batch decode step, the accelerator may read a large fraction of model weights to produce a small amount of arithmetic output. If bytes must be moved faster than HBM can supply them, adding arithmetic units does little. Higher batching reuses weights across more tokens and raises arithmetic intensity."},{title:"Memory wall",body:"Compute capability has historically improved faster than the ability to move data economically. Optimisations such as FlashAttention, quantization, caching, near-memory processing and larger on-chip SRAM are attempts to reduce expensive data movement."}],equation:{formula:"Weight memory ≈ parameters × bytes per parameter",note:"A 70B-parameter dense model needs roughly 140 GB at 16-bit, 70 GB at 8-bit or 35 GB at 4-bit before runtime overhead, activations and KV cache. Actual deployment adds headroom and may shard weights across devices."},terms:["register","SRAM","cache","DRAM","HBM","DDR","NAND","bandwidth","latency","memory wall","arithmetic intensity"],investor:{what:"Memory content per accelerator and the difficulty of qualifying high-yield stacks matter more than generic DRAM bit demand alone.",priced:"HBM scarcity can be priced as structurally permanent even while conventional DRAM remains cyclical; both views can be partly true.",indicators:"HBM content per system, stack count, bits per stack, yields, qualification, conventional DRAM wafer allocation, DDR pricing and cache efficiency.",trap:"Treating all memory bits as interchangeable or assuming capacity automatically solves bandwidth.",secondOrder:"More wafer allocation to HBM can tighten ordinary DRAM; better cache compression can lower HBM per request but unlock much higher concurrency.",invalidation:"A structural HBM thesis weakens if architectures materially reduce off-chip data movement or supply/yield improves faster than content growth."},quiz:[{q:"Which memory is normally fastest but least dense?",options:["NAND","DDR DRAM","SRAM","Hard disk"],answer:2,explanation:"SRAM is used close to compute because of speed, but consumes much more area per bit than DRAM."},{q:"A 100B-parameter model stored at 8 bits per parameter requires approximately how much memory for weights alone?",options:["12.5 GB","100 GB","800 GB","1 TB per token"],answer:1,explanation:"Eight bits equal one byte, so 100 billion parameters require roughly 100 billion bytes, or about 100 GB before overhead."},{q:"Why can adding more arithmetic units fail to speed up decode?",options:["The workload may be limited by memory bandwidth","Arithmetic units reduce context length","Decode never uses weights","HBM is persistent storage"],answer:0,explanation:"If the processor is waiting for weights or cache data, additional compute remains underutilised."}]},{id:6,stage:"Hardware",time:"45 min",title:"HBM, the base die and memory-on-logic",subtitle:"From standard memory to deeper 