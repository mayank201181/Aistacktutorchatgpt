e=>"All"===a.companyLayer||e.layer===a.companyLayer);return`<div class="section-head"><div><div class="eyebrow">Illustrative value chain</div><h2>Who makes what</h2><p>Use this as a causal map, not a recommendation list. Roles and product roadmaps change; verify current filings.</p></div></div>\n      <div class="filter-row">${t.map(e=>`<button class="filter-pill ${a.companyLayer===e?"active":""}" data-company-layer="${o(e)}">${o(e)}</button>`).join("")}</div>\n      <div class="company-grid">${s.map(e=>`<article class="company-card"><div class="company-head"><div><div class="company-name">${o(e.name)}</div><div class="company-layer">${o(e.layer)}</div></div><div class="ticker">${o(e.ticker)}</div></div><dl><dt>Role</dt><dd>${o(e.role)}</dd><dt>Potential moat</dt><dd>${o(e.moat)}</dd><dt>Watch</dt><dd>${o(e.watch)}</dd><dt>Risk</dt><dd>${o(e.risk)}</dd></dl></article>`).join("")}</div>`}();case"articles":return`<div class="section-head"><div><div class="eyebrow">Capstone</div><h2>Article lab</h2><p>Read each thesis as a chain of evidence, not as a persuasive narrative. Your notes and conviction are saved locally.</p></div></div>\n      <div class="article-grid">${e.articles.map(e=>{const t=i.articleConvictions[e.id]??50,s=String(i.articleNotes[e.id]||"").trim().length>0;return`<article class="card article-card"><div style="display:flex;justify-content:space-between"><span class="source">${o(e.source)}</span><span class="date">${o(e.date)}</span></div><h3>${o(e.title)}</h3><p>${o(e.thesis)}</p><div class="badge-row" style="margin-top:8px"><span class="badge orange">Conviction ${t}/100</span>${s?'<span class="badge green">Notes saved</span>':""}</div><div class="article-actions"><button class="btn primary" data-article="${e.id}">Analyse</button><a class="btn ghost" href="${e.url}" target="_blank" rel="noopener">Original ↗</a></div></article>`}).join("")}</div>\n      <section class="section"><div class="callout blue"><h3>How to use the capstone</h3><p>First state the author's claim without critique. Then identify the factual observations, the assumptions that are not yet observed, the economic inference and the trade. Finally, write the strongest counterargument and one measurable falsifier.</p></div></section>`;case"article":return function(){const t=e.articles.find(e=>e.id===a.articleId),s=i.articleConvictions[t.id]??50,n=i.articleNotes[t.id]||"",r=[["Fact",t.chain.fact],["Assumption",t.chain.assumption],["Inference",t.chain.inference],["Market expression",t.chain.expression]];return`<div class="module-header"><div><div class="module-kicker"><span class="module-number">ARTICLE LAB</span><span class="stage-pill markets">${o(t.source)}</span><span style="color:var(--faint);font-size:11px">${o(t.date)}</span></div><h2>${o(t.title)}</h2><p class="module-summary">${o(t.thesis)}</p></div><div class="module-actions"><button class="btn ghost" data-nav="articles">← All articles</button><a class="btn" href="${t.url}" target="_blank" rel="noopener">Read original ↗</a></div></div>\n      <section class="section"><div class="section-head"><div><h3>Claim chain</h3><p>Where the argument moves from observation to assumption is usually where the equity risk sits.</p></div></div><div class="claim-chain">${r.map(([e,t])=>`<div class="claim-box"><div class="type">${e}</div><div class="claim">${o(t)}</div></div>`).join("")}</div></section>\n      <section class="section grid two"><div class="callout good"><h3>What it gets right</h3><ul class="clean-list compact">${t.getsRight.map(e=>`<li>${o(e)}</li>`).join("")}</ul></div><div class="callout warn"><h3>Where the argument is fragile</h3><ul class="clean-list compact">${t.fragile.map(e=>`<li>${o(e)}</li>`).join("")}</ul></div></section>\n      <section class="section grid two"><div class="card"><h3>Monitor to arbitrate the thesis</h3><ul class="clean-list">${t.monitor.map(e=>`<li>${o(e)}</li>`).join("")}</ul></div><div class="card investor-lens"><div class="eyebrow">Candidate expression</div><p style="margin-top:8px;color:#d2d4d8">${o(t.exposures)}</p><hr><h4>Questions the author should answer</h4><ul class="clean-list compact">${t.questions.map(e=>`<li>${o(e)}</li>`).join("")}</ul></div></section>\n      <section class="section"><div class="card"><h3>Your thesis ledger</h3><div class="conviction" style="margin:14px 0 18px"><span>Reject</span><input type="range" min="0" max="100" value="${s}" data-article-conviction="${t.id}"><span><strong id="conviction-value">${s}</strong>/100</span></div><textarea data-article-notes="${t.id}" placeholder="My base case, what is priced, strongest counterargument, expression, risk and falsifier…">${o(n)}</textarea><div style="color:var(--faint);font-size:10px;margin-top:7px">Saved automatically in this browser.</div></div></section>`}();case"socratic":return function(){const t=e.socratic[a.socraticIndex%e.socratic.length],s=i.socratic||{seen:0,gotIt:0,needWork:0},n=p(t.module);return`<div class="section-head"><div><div class="eyebrow">Active recall</div><h2>Socratic review</h2><p>Answer aloud before revealing. The goal is a causal explanation, not vocabulary recognition.</p></div><div class="badge-row"><span class="badge">Seen ${s.seen||0}</span><span class="badge green">Got it ${s.gotIt||0}</span><span class="badge orange">Need work ${s.needWork||0}</span></div></div>\n      <div class="socratic"><div class="card socratic-card"><div class="prompt-label">Module ${String(n.id).padStart(2,"0")} · ${o(n.title)}</div><div class="prompt">${o(t.prompt)}</div>${a.showHint?`<div class="hint"><strong>Hint:</strong> ${o(t.hint)}</div>`:""}${a.showAnswer?`<div class="answer"><strong>Model answer:</strong> ${o(t.answer)}</div>`:""}<div class="socratic-actions">${a.showHint?"":'<button class="btn ghost" data-socratic="hint">Show hint</button>'}${a.showAnswer?'<button class="btn bad" data-socratic-rate="needWork">Need work</button><button class="btn good" data-socratic-rate="gotIt">Got it</button>':'<button class="btn" data-socratic="answer">Reveal answer</button>'}<button class="btn primary" data-socratic="next">Next prompt</button></div></div></div>`}();case"assessment":return function(){const e=z(),t=!0===a.assessmentGraded,s=e.map((e,s)=>{const i=a.assessmentSelections[s];return`<div class="question"><div class="q-num">Module ${String(e.module.id).padStart(2,"0")} · ${o(e.module.title)}</div><div class="q-text">${o(e.q.q)}</div><div class="options">${e.q.options.map((a,n)=>{const r=["option"];return i===n&&r.push("selected"),t&&n===e.q.answer&&r.push("correct"),t&&i===n&&n!==e.q.answer&&r.push("wrong"),`<label class="${r.join(" ")}" data-assessment-answer="${s}:${n}"><input type="radio" name="a-${s}" ${i===n?"checked":""} ${t?"disabled":""}><span>${o(a)}</span></label>`}).join("")}</div>${t?`<div class="explanation">${o(e.q.explanation)}</div>`:""}</div>`}).join(""),n=Object.keys(a.assessmentSelections).length,r=t?e.reduce((e,t,s)=>e+(a.assessmentSelections[s]===t.q.answer?1:0),0):null;return`<div class="section-head"><div><div class="eyebrow">Synthesis</div><h2>Final assessment</h2><p>Twelve questions—one from each module. Use it as a diagnostic before or after the course.</p></div>${null!==i.assessmentScore?`<span class="score-chip">Best ${i.assessmentScore}/${e.length}</span>`:""}</div><div class="quiz">${s}<div class="card quiz-score"><div><h3>${t?`Score: ${r}/${e.length}`:`${n}/${e.length} answered`}</h3><p style="color:var(--muted);font-size:11px">9/12 suggests working fluency; the article lab is the more important test of investment judgment.</p></div>${t?'<button class="btn" data-assessment-reset>Retake</button>':`<button class="btn primary" data-assessment-submit ${n<e.length?"disabled":""}>Grade assessment</button>`}</div></div>`}();case"sources":return`<div class="section-head"><div><div class="eyebrow">Method & provenance</div><h2>Sources and limitations</h2><p>Primary technical sources are preferred where practical; the article lab critiques the three supplied opinion pieces.</p></div></div>\n      <div class="grid two"><div class="card"><h3>How the course is built</h3><ul class="clean-list"><li>Technical mechanisms are simplified enough to reason with, but calculators expose assumptions rather than hiding them.</li><li>Company mapping is illustrative and not exhaustive. Verify current products, segment reporting and customer relationships.</li><li>All prices in labs are user-entered placeholders; nothing in the app is a live quote or forecast.</li><li>The article critique separates facts, assumptions, inferences and market expressions. A technically correct claim can still imply the wrong security or timing.</li></ul></div><div class="card"><h3>Important limitations</h3><ul class="clean-list"><li>Training and serving performance are architecture- and software-specific; simple FLOP and bandwidth equations are scale checks.</li><li>Product roadmaps, licences, capex plans and contract terms can change after the review date.</li><li>Private contract quality, yields and customer concentration may not be fully observable.</li><li>Educational content only; use filings and primary company disclosures before trading.</li></ul><div class="badge-row"><span class="badge orange">Reviewed ${o(e.meta.reviewed)}</span><span class="badge">Version ${o(e.meta.version)}</span></div></div></div>\n      <section class="section"><div class="card"><h3>Reading list</h3><div class="source-list">${e.sources.map(e=>`<div class="source-item"><div class="s-tag">${o(e.category)}</div><div><div class="s-title">${o(e.title)}</div><div class="s-org">${o(e.org)}</div></div>${"#"===e.url?'<span class="badge">Internal synthesis</span>':`<a class="btn small ghost" href="${e.url}" target="_blank" rel="noopener">Open ↗</a>`}</div>`).join("")}</div></section>`;default:return function(){const t=m(),s=function(){const t=e.modules.map(e=>v(e.id).score).filter(e=>Number.isFinite(e));return t.length?Math.round(t.reduce((e,t)=>e+t,0)/(3*t.length)*100):null}(),a=p(function(){const t=e.modules.find(e=>!v(e.id).completed);return t?t.id:12}()),n=Object.keys(i.articleNotes).filter(e=>String(i.articleNotes[e]||"").trim()).length>0?1:0,r=e.modules.map(e=>{const t=v(e.id);return`<article class="card hover path-card" data-module="${e.id}">\n        <div class="path-top"><div class="path-num">${String(e.id).padStart(2,"0")}</div><div><span class="stage-pill ${y(e.stage)}">${o(e.stage)}</span><h3>${o(e.title)}</h3><p>${o(e.subtitle)}</p></div></div>\n        <div class="path-bottom"><span>${o(e.time)}</span><span>${t.completed?"Completed":Number.isFinite(t.score)?`Quiz ${t.score}/3`:"Not started"}</span></div>\n      </article>`}).join("");return`<section class="hero">\n      <div class="eyebrow">From silicon to software economics</div>\n      <h1>Understand the AI stack.<br>Trade the bottlenecks, not the buzzwords.</h1>\n      <p>A twelve-module path for an investor: plain-English analogies, technical mechanics, interactive unit-economics labs, quizzes and a capstone critique of the three articles you supplied.</p>\n      <div class="hero-actions"><button class="btn primary" data-module="${a.id}">${t?"Continue":"Start"}: Module ${String(a.id).padStart(2,"0")}</button><button class="btn" data-nav="articles">Open the article lab</button><button class="btn ghost" data-nav="assessment">Take the diagnostic</button></div>\n    </section>\n\n    <section class="section grid four">\n      <div class="card metric"><div class="label">Course progress</div><div style="display:flex;align-items:center;gap:18px;margin-top:9px"><div class="progress-ring" style="--p:${h()}"><span>${h()}%</span></div><div><div class="value">${t}/${e.modules.length}</div><div class="sub">modules completed</div></div></div></div>\n      <div class="card metric"><div class="label">Quiz average</div><div class="value">${null===s?"—":s+"%"}</div><div class="sub">Pass threshold: 2 of 3 per module</div><div class="spark">${[35,52,45,68,73,82,90].map((e,t)=>`<span class="${t>4?"hot":""}" style="height:${e}%"><