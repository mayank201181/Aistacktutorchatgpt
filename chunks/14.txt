/span>`).join("")}</div></div>\n      <div class="card metric"><div class="label">Working vocabulary</div><div class="value">${e.glossary.length}</div><div class="sub">searchable concepts, acronyms and metrics</div><div class="spark">${[28,33,40,55,69,78,96].map((e,t)=>`<span class="${t>4?"hot":""}" style="height:${e}%"></span>`).join("")}</div></div>\n      <div class="card metric"><div class="label">Article capstones</div><div class="value">${n}/3</div><div class="sub">articles with saved investment notes</div><div class="spark">${[20,20,35,35,55,55,85].map((e,t)=>`<span class="${t>4?"hot":""}" style="height:${e}%"></span>`).join("")}</div></div>\n    </section>\n\n    <section class="section">\n      <div class="section-head"><div><h2>A three-pass method</h2><p>Build the map, test the mechanics, then force every narrative through an investment ledger.</p></div></div>\n      <div class="grid three">\n        <div class="card"><div class="eyebrow">01 · Learn</div><h3>Concept → analogy → mechanism</h3><p style="color:var(--muted);font-size:12px">Each module starts in plain English, then adds the minimum technical detail needed to interpret products, earnings and industry claims.</p></div>\n        <div class="card"><div class="eyebrow">02 · Test</div><h3>Play with the variables</h3><p style="color:var(--muted);font-size:12px">Change precision, token mix, yield, utilization, PUE or model cost and watch the economics move. Finish with a three-question check.</p></div>\n        <div class="card"><div class="eyebrow">03 · Apply</div><h3>Fact → assumption → inference → trade</h3><p style="color:var(--muted);font-size:12px">The article lab separates technical truth from the economic claim, highlights the strongest counterargument and identifies falsifiers.</p></div>\n      </div>\n    </section>\n\n    <section class="section">\n      <div class="section-head"><div><h2>The learning path</h2><p>${o(e.meta.estimatedHours)} at a deliberate pace. Modules are independent, but the sequence compounds.</p></div><button class="btn small" data-nav="glossary">Open glossary</button></div>\n      <div class="grid three">${r}</div>\n    </section>\n\n    <section class="section grid two">\n      <div class="card investor-lens"><div class="eyebrow">Investor default</div><h3 style="margin-top:7px">Structural demand can be real while the equity trade is wrong.</h3><p style="color:#d0d2d7;font-size:12px">Separate end demand from supplier revenue, revenue from gross profit, and average economics from the marginal project. Track the bottleneck migration and the second derivative of estimates.</p></div>\n      <div class="card"><div class="eyebrow">Next best action</div><h3 style="margin-top:7px">${t?`Continue with ${o(a.title)}`:"Start with the master map"}</h3><p style="color:var(--muted);font-size:12px">Do the lab before the quiz. Record one market implication and one falsifier in the module notes.</p><button class="btn primary" data-module="${a.id}">Open Module ${String(a.id).padStart(2,"0")}</button></div>\n    </section>`}()}}()}</div></main>`,document.querySelector(".toast")?.remove(),document.body.insertAdjacentHTML("beforeend",'<div class="toast" id="toast"></div>')}function k(e,t,s,a){return`<div class="lab"><div class="lab-head"><div><h3>${e}</h3><p>${t}</p></div><span class="badge orange">Change the inputs</span></div>${s}<div class="result-panel" id="lab-result">${a}</div></div>`}function x(e,t,s,a,i,n,o,r=""){return`<div class="control"><label>${s}<output data-output="${t}">${c(Number(o),n<1?1:0)}${r}</output></label><input type="range" min="${a}" max="${i}" step="${n}" value="${o}" data-lab-id="${e}" data-lab-key="${t}" data-suffix="${r}"></div>`}function S(e,t,s,a,i=0,n=1){return`<div class="control"><label>${s}</label><input type="number" min="${i}" step="${n}" value="${a}" data-lab-id="${e}" data-lab-key="${t}"></div>`}function C(e,t,s,a,i){return`<div class="control"><label>${s}</label><select data-lab-id="${e}" data-lab-key="${t}">${i.map(([e,t])=>`<option value="${e}" ${String(a)===String(e)?"selected":""}>${t}</option>`).join("")}</select></div>`}function q(e){return`<div style="display:grid;gap:9px;margin-top:12px">${Object.entries(e).sort((e,t)=>t[1]-e[1]).map(([e,t],s)=>`<div><div style="display:flex;justify-content:space-between;font-size:11px"><strong>${e}</strong><span>${Math.round(t)}/100</span></div><div class="progress-mini" style="height:7px"><span style="width:${r(t,0,100)}%;background:${0===s?"var(--accent)":"#596270"}"></span></div></div>`).join("")}</div>`}function A(e){const t=a.labs[e];if(1===e){const e=s[t.node];return`<div class="eyebrow">${String(t.node+1).padStart(2,"0")} · ${e.title}</div><div class="result-big" style="font-size:20px;margin-top:6px">${e.detail}</div><div class="callout warn" style="margin-top:13px"><h3>Investor question</h3><p>${e.question}</p></div>`}if(2===e){const e=55+.3*t.volatility+.12*t.latency-.18*t.volume-.15*t.regularity,s=48+.24*t.volume+.18*t.volatility+.15*t.latency-.05*Math.abs(t.regularity-65),a=22+.36*t.regularity+.34*t.volume+.18*t.latency-.34*t.volatility,i={"CPU / general compute":r(e,0,100),"GPU platform":r(s,0,100),"Custom ASIC":r(a,0,100)};return`<div class="result-big">${Object.entries(i).sort((e,t)=>t[1]-e[1])[0][0]}</div><p style="color:var(--muted);font-size:11px">Highest directional fit. A real decision must add software migration, memory, networking, availability and utilization.</p>${q(i)}`}if(3===e){const e=6*t.params*1e9*t.tokens*1e9,s=t.gpus*t.tflops*1e12*(t.utilization/100),a=e/Math.max(s,1),i=a/86400,n=t.gpus*t.power*(a/3600)/1e3,o=t.gpus*t.power/1e3;return`<div class="result-big">≈ ${c(i,1)} training days</div><div class="result-row"><span>Approximate training FLOPs (6ND)</span><strong>${e.toExponential(2)}</strong></div><div class="result-row"><span>Delivered cluster compute</span><strong>${l(s)} FLOP/s</strong></div><div class="result-row"><span>Accelerator load</span><strong>${c(o,1)} MW</strong></div><div class="result-row"><span>Accelerator energy only</span><strong>${c(n/1e3,1)} GWh</strong></div><p style="color:var(--faint);font-size:10px;margin-top:9px">Excludes CPUs, networking, cooling, failed runs, data pipeline and facility overhead. MoE active parameters and architecture can materially change the estimate.</p>`}if(4===e){const e=30,s=t.requests*e*t.inputTokens,a=t.requests*e*t.outputTokens,i=s*(1-t.cacheHit/100)/1e6*t.inputPrice,n=a/1e6*t.outputPrice,o=i+n,r=o?n/o*100:0,c=s+a;return`<div class="result-big">${d(o)} / month</div><div class="result-row"><span>Total tokens</span><strong>${l(c)}</strong></div><div class="result-row"><span>Input cost after cache</span><strong>${d(i)}</strong></div><div class="result-row"><span>Output / decode cost</span><strong>${d(n)} · ${u(r)}</strong></div><div class="result-row"><span>Cost per request</span><strong>${d(o/(t.requests*e),4)}</strong></div><div class="callout ${r>65?"warn":"blue"}" style="margin-top:12px"><h3>${r>65?"Decode dominates the bill":"Prompt mix is material"}</h3><p>${r>65?"Output tokens are expensive and sequential. Shorter answers, smaller models and speculative decoding may matter more than prompt caching.":"Input processing and retrieval/caching choices have meaningful leverage. Inspect time-to-first-token as well as total cost."}</p></div>`}if(5===e){const e=t.params*t.bytes,s=t.kv*t.concurrency,a=e+s,i=t.hbm*t.accelerators,n=i-a,o=t.bandwidth*t.accelerators*1e3,r=e>0?o/e:0;return`<div class="result-big" style="color:${n>=0?"var(--good)":"var(--bad)"}">${n>=0?"Fits with "+c(n,0)+" GB headroom":"Short by "+c(-n,0)+" GB"}</div><div class="result-row"><span>Weight memory</span><strong>${c(e,1)} GB</strong></div><div class="result-row"><span>KV cache</span><strong>${c(s,1)} GB</strong></div><div class="result-row"><span>Available HBM</span><strong>${c(i,0)} GB</strong></div><div class="result-row"><span>Very rough bandwidth ceiling</span><strong>${c(r,1)} weight-streams/s</strong></div><p style="color:var(--faint);font-size:10px;margin-top:9px">The bandwidth figure assumes each generated token streams the full weight set once and ignores tensor parallel communication, caching, kernels and utilization. It is a diagnostic, not a forecast.</p>`}if(6===e){const e=Array.from({length:t.layers},(e,t)=>`<div class="hbm-layer">DRAM ${t+1}</div>`).join(""),s=t.custom?'<div class="hbm-layer logic">Customer logic</div>':"",a="logic"===t.base?"Logic base die":"DRAM-process base",i=("logic"===t.base?2:0)+(t.custom?2:0)+(t.layers>=16?1:0),n=(t.custom?2:0)+(t.layers>=12?1:0)+(t.layers>=16?1:0);return`<div class="hbm-stack"><div class="hbm-visual"><div class="hbm-layer base">${a}</div>${s}${e}</div><div class="hbm-notes"><div><strong>Capacity:</strong> ${t.layers}-high stack raises usable memory per package, subject to die density and product configuration.</div><div><strong>Differentiation score:</strong> ${i}/5. Logic and customer-specific circuitry can deepen co-design.</div><div><strong>Execution-risk score:</strong> ${n}/4. Stack height and customization raise yield, testing and concentration risk.</div><div><strong>Valuation gate:</strong> prove who owns the design rent and whether margins remain resilient after supply expands.</div></div></div>`}if(7===e){const e=Math.pow(t.computeYield/100,t.computeDies)*Math.pow(t.hbmYield/100,t.hbmStacks)*(t.assemblyYield/100),s=t.computeDies*t.computeCost+t.hbmStacks*t.hbmCost+t.assemblyCost,a=s/Math.max(e,1e-4),i=a-s;return`<div class="result-big">${u(100*e,1)} package yield</div><div class="result-row"><span>Raw component + assembly cost</span><strong>${d(s)}</strong></div><div class="result-row"><span>Expected cost per good package</span><strong>${d(a)}</strong></div><div class="result-row"><span>Yield-loss burden per good unit</span><strong>${d(i)}</strong></div><div class="callout ${e<.35?"bad":e<.6?"warn":"good"}" style="margin-top:12px"><h3>${e<.35?"Compounded yield dominates":"Known-good-die strategy matters"}</h3><p>Independent-yield multiplication is deliberately harsh. Real flows use pre-test, redundancy, binning and repair, but late package failures can still destroy high-value inputs.</p></div>`}if(8===e){const e=55+.25*t.urgency+.2*(100-t.stability)+.1*(100-t.software)+.1*t.scale,s=18+.32*t.stability+.28*t.scale+.23*t.power+.15*t.software-.18*t.urgency,a=24+.2*t.scale+.22*t.power+.18*t.software+.1*t.stability-.08*t.urgency,i={"Flexible GPU platform":r(e,0,100),"Custom ASIC":r(s,0,100),"Alternative / wafer-scale":r(a,0,100)};return`<div class="result-big">${Object.entries(i).sort((e,t)=>t[1]-e[1])[0][0]}</div><p style="color:var(--muted);font-size:11px">Directional architecture fit. Require deployed utilization, software evidence and whole-system TCO before turning it into an equity claim.</p>${q(i)}`}if(9===e){const e=t.itLoad*t.pue,s=8760*e,a=s*t.powerPrice,i=t.itLoad*t.utilization/100,n=1e3*t.itLoad/t.gpuPower,o=t.itLoad-i;return`<div class="result-big">${c(e,1)} MW facility draw</div><div class="result-row"><span>Annual electricity</span><strong>${c(s/1e6,2)} TWh</strong></div><div class="result-row"><span>Annual electricity cost</span><strong>${d(a/1e6,1)}m</strong></div><div class="result-row"><span>Approx. installed accelerator systems</span><strong>${l(n)}</strong></div><div class="result-row"><span>Productive IT load at utilization</span><strong>${c(i,1)} MW</strong></div><div class="result-row"><span>Idle-equivalent IT load</span><strong>${c(o,1)} MW</strong></div><p style="color:var(--faint);font-size:10px;margin-top:9px">Excludes demand charges, transmission, backup generation, curtailment, equipment mix and non-accelerator IT load.</p>`}if(10===e){const e=55+.3*t.frontier+.22*(100-t.engineering)+.18*(100-t.control)+.12*(100-t.privacy)-.08*t.portability,s=42+.18*t.privacy+.18*t.control+.2*t.scale+.16*t.portability+.1*t.frontier,a=15+.28*t.privacy+.28*t.control+.22*t.engineering+.18*t.scale+.12*t.portability-.12*t.frontier,i={"Closed frontier API":r(e,0,100),"Managed open-weight endp